

export const STARKNET_RPC_URL = '';

export const MOVE_TO_CEX = false; // Move funds to CEX

export const MAX_AMOUNT_TO_KEEP = 7.5; // Max amount to keep on wallet in USD

export const MIN_AMOUNT_TO_KEEP = 7; // Min amount to keep on wallet in USD

export const MAX_TRANSACTIONS_PER_WALLET = 10; // Max transactions per wallet

export const MIN_WAIT_TIME = 0.2; // Min wait time between transactions (minutes)

export const MAX_WAIT_TIME = 1.1; // Max wait time between transactions (minutes)

export let TG_CHAT_ID = 0; // Telegram chat id

export let TG_TOKEN = ''; // Telegram bot token

export const ENABLE_MODULES = {
  CARMINE: true,
  DMAIL: true,
  STARK_ID: true,
  STARKVERSE: true,
  UNFRAMED_BID: true,
  ZKLEND_COLLATERAL: true,
  GOL2_EVOLVE: true,
}